% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_data.R
\name{cmhc_resolve_non_market_zone_ids}
\alias{cmhc_resolve_non_market_zone_ids}
\title{Attach zone_id to Parsed Zone-Level Data}
\usage{
cmhc_resolve_non_market_zone_ids(df, zones_lookup, cma_to_met, cma_ref)
}
\arguments{
\item{df}{A tibble with columns \code{geo_name} (zone name) and
\code{cma_sheet} (the xlsx sheet the zone came from).}

\item{zones_lookup}{A data.frame from \code{cmhc_zone_lookup()}.}

\item{cma_to_met}{A data.frame with \code{cma_uid} and \code{met_code}
(from \code{cmhc_get_cma_to_met()}).}

\item{cma_ref}{A tibble from \code{cmhc_build_cancensus_cma_ref()}.}
}
\value{
The input \code{df} with an added \code{zone_id} column (NA for
unmatched zones).
}
\description{
Resolves each zone to its official
\code{survey_zone_geographic_layer_id} using the 3-pass matcher
\code{cmhc_match_zone_name()}. The \code{met_code} required by the
matcher comes from joining each row's parent \code{cma_id} (resolved
from \code{cma_sheet}) against \code{cma_to_met}.
}
