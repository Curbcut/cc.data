% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osm_places.R
\name{osm_categories_default}
\alias{osm_categories_default}
\title{Default OSM category configuration}
\usage{
osm_categories_default()
}
\value{
A list of category specifications.
}
\description{
Returns the list of categories used by \code{\link[=osm_extract_canada]{osm_extract_canada()}}. Each entry
is a list with:
\describe{
\item{type_id}{character — category id, used in filenames and the
returned data}
\item{where}{character — OGR SQL \code{WHERE} clause filtering features}
\item{tags}{character vector — extra OSM tags to retain in the .gpkg}
\item{strategy}{\code{"polys_only"} or \code{"pt_in_poly"} — how to deduplicate
when the same feature appears as both a point and a
polygon}
}
}
\details{
Users may override entries or extend the list to add their own categories.
}
