% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_data.R
\name{cmhc_read_variable}
\alias{cmhc_read_variable}
\title{Read a single variable from a directory (or S3 prefix) of per-task .qs files}
\usage{
cmhc_read_variable(dir, variable, min_file_size = 69L, geo_level = NULL)
}
\arguments{
\item{dir}{Directory containing \code{.qs} task files, or S3 key prefix.}

\item{variable}{Character. The variable key to extract (e.g.
\code{"vacancy_rate_bedroom_type_total"}).}

\item{geo_level}{Character or NULL. When \code{"ct"}, IDs are validated against
\code{geography.ct} per \code{geo_vintage} and rows with unknown IDs are dropped.
Pass \code{NULL} (default) to skip validation.}
}
\value{
A single data.frame (finalized: \code{id} column, character), or
\code{NULL} if the variable is not found in any file.
}
\description{
Each task file is a named list of data.frames (one per variable key).
This function reads files one at a time, extracts only the requested
variable, and combines the rows.  Peak RAM is one task file plus the
accumulated rows for that single variable.
}
\details{
When \code{options(cmhc.s3_bucket)} is set, \code{dir} is treated as an S3 key
prefix and files are streamed from the bucket.
}
