% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/travel_time_duckdb.R
\name{tt_query}
\alias{tt_query}
\title{Query travel times from the local DuckDB database}
\usage{
tt_query(con, origin_ids, dest_ids = NULL, mode = "bicycle", max_time_s = NULL)
}
\arguments{
\item{con}{<\code{DBIConnection}> Connection returned by \code{\link[=tt_connect]{tt_connect()}}.}

\item{origin_ids}{<\code{character}|\code{integer64}|\code{numeric}> Vector of origin
dissemination-block UIDs to query.}

\item{dest_ids}{<\code{character}|\code{integer64}|\code{numeric}|\code{NULL}> Optional vector
of destination UIDs to restrict results. \code{NULL} returns all reachable
destinations.}

\item{mode}{<\code{character}> Transport mode table to query. One of \code{"car"},
\code{"bicycle"}, \code{"foot"}. Default \code{"bicycle"}.}

\item{max_time_s}{<\code{integer}|\code{NULL}> Optional upper bound on travel time in
seconds. \code{NULL} imposes no limit.}
}
\value{
A list with two elements:
\describe{
\item{\code{data}}{A \code{data.frame} with columns \code{origin_db}, \code{dest_db},
\code{time_s}, \code{dist_m}.}
\item{\code{elapsed_sec}}{Query wall-clock time in seconds.}
}
}
\description{
Filters the \verb{ttm_\{mode\}} table by one or more origin IDs and optionally
by destination IDs and/or a maximum travel time.
}
\section{ID encoding}{

IDs are passed through \code{as.character()} before interpolation into SQL to
prevent R's \code{double} representation from producing scientific notation
(\code{1e10}) for 11-digit UIDs, which silently returns zero rows.
}

\section{Performance note}{

The local DuckDB table is sorted by \code{origin_db}. DuckDB zone maps let
narrow origin-range queries skip entire row groups, so queries for
geographically clustered origins (same CSD or CMA) are fast even on
large tables. For large destination filters, prefer the S3 track
(\code{\link[=tt_query_s3]{tt_query_s3()}}) or filter post-hoc in R.
}

