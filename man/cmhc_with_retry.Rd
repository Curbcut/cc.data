% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_data.R
\name{cmhc_with_retry}
\alias{cmhc_with_retry}
\title{Fetch CMHC Data for a Specific CMA}
\usage{
cmhc_with_retry(.f, max_retries = 1, base_delay = 2, label = "")
}
\arguments{
\item{max_retries}{Integer. Maximum number of retry attempts (default 1).}

\item{base_delay}{Numeric. Initial delay in seconds before first retry (default 2).}

\item{label}{Character. Label for warning messages (e.g. "CMA 24462").}

\item{survey}{Character. The CMHC survey code (e.g., "Rms", "Scss").}

\item{series}{Character. The series to extract (e.g., "Vacancy Rate").}

\item{dimension}{Character. The breakdown dimension (e.g., "Bedroom Type"). Can be \code{NULL}.}

\item{geo_uid}{Character. Geographic identifier of the CMA (e.g., "24462").}

\item{frequency}{Character. Must be either "Monthly" or "Annual".}

\item{expr}{An expression (typically a \code{cmhc::get_cmhc()} call).}
}
\value{
A raw CMHC \code{data.frame}, or \code{NULL} if an error occurs.
Retry wrapper for CMHC API calls

Retries a CMHC API call with exponential backoff when transient errors occur
(HTTP 500, rate limiting, connection timeouts). Non-retryable errors (e.g.
"No data available") are returned as-is on the first attempt.

The result of \code{expr}, or \code{NULL} if all retries fail.
}
\description{
Downloads CMHC data for a given CMA using specified parameters. Handles API errors
gracefully using \code{tryCatch}, and returns \code{NULL} if the query fails.
}
