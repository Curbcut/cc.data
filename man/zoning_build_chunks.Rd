% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zoning.R
\name{zoning_build_chunks}
\alias{zoning_build_chunks}
\title{Build the chunk task grid — one plain integer row per chunk}
\usage{
zoning_build_chunks(zones, chunk_size = 1000L)
}
\arguments{
\item{zones}{An sf zoning object with \code{zone_row}.}

\item{chunk_size}{Integer. Zones per chunk. Default 1000.}
}
\value{
A data.frame with one \code{chunk_id} per chunk.
}
\description{
Passes only a chunk index to each worker (a simple integer column, which
\code{mirai_map} handles cleanly). The worker derives its own slice of zone_rows
from chunk_id + chunk_size, so no list-column crosses the daemon boundary.
}
