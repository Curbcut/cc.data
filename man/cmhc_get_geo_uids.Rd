% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_data.R
\name{cmhc_get_geo_uids}
\alias{cmhc_get_geo_uids}
\title{Build a flat task dataframe for parallel CMHC processing}
\usage{
cmhc_get_geo_uids(geo_level, vintage = "2021")
}
\arguments{
\item{geo_level}{Character. One of \code{"cma"}, \code{"csd"}, \code{"ct"}, etc.}

\item{vintage}{Character. Census vintage (default \code{"2021"}).}
}
\value{
Character vector of unique geographic IDs.
}
\description{
Creates one row per independent unit of work from all combinations of
geographic IDs and request parameters. Used with \code{mirai::mirai_map()}.
}
\details{
Get geographic UIDs from the database (no geometry loaded)

Queries the \verb{geography.<geo_level>} table for distinct IDs at the 2021
vintage. This avoids loading full shapefiles just to get a list of UIDs.

For CMA, combines both \code{geography.cma} and \code{geography.cmasplit}, then
removes the short (3-digit) merged cross-provincial IDs so that only
province-qualified CMAPUID values remain.
}
