% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_update.R
\name{cmhc_plan}
\alias{cmhc_plan}
\title{Build the refresh plan for one scale (read-only)}
\usage{
cmhc_plan(
  con,
  scale,
  uids = NULL,
  registry = cmhc_registry(),
  levels = cmhc_levels()
)
}
\arguments{
\item{con}{A live DBI connection.}

\item{scale}{One of \code{"cma"}, \code{"csd"}, \code{"ct"}, \code{"zone"}, \code{"nbhd"}.}

\item{uids}{Optional geo (or CMA parent) UIDs. Defaults to the scale's own.}

\item{registry}{Request registry. Defaults to \code{\link[=cmhc_registry]{cmhc_registry()}}.}

\item{levels}{Scale config table. Defaults to \code{\link[=cmhc_levels]{cmhc_levels()}}.}
}
\value{
A data frame, one row per variable x frequency, with \code{scale},
\code{variable_id}, \code{freq}, \code{db_latest}, \code{remote_latest}, \code{to_fetch}, \code{window},
and \code{est_calls}.
}
\description{
Computes, for every variable x frequency at one scale, the latest stored and
published periods, the resulting gap window, and an estimate of the API calls
a real fetch would make. Performs no network fetch beyond the cached national
availability probe and no upload.
}
