% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zoning.R
\name{zoning_launch_usages}
\alias{zoning_launch_usages}
\title{Compute per-DA land area for every usage (chunked by CMA)
Computes per-DA area for each usage in parallel. The work is split into one
task per (usage x CMA) pair — e.g. 12 usages x 11 CMAs = 132 small tasks.}
\usage{
zoning_launch_usages(zones, da, usages = ZONING_USAGE_TYPES)
}
\arguments{
\item{zones}{An \code{sf} object of zoning polygons. Must have a \code{cma_id} column.}

\item{da}{An \code{sf} object of dissemination areas with \code{id} and \code{cma_id}.}

\item{usages}{Character vector of usage types (default \code{ZONING_USAGE_TYPES}).}
}
\value{
A list with \code{mp} (mirai_map object), \code{tasks} (data.frame), and
\code{usages}. Run \code{res <- mp[.progress]} then
\code{zoning_combine_usages(res, tasks, usages)}.
}
\description{
IMPORTANT: the native \verb{[.progress} green ETA bar only renders when the
\code{mp[.progress]} call happens at the TOP LEVEL of an interactive script, not
when buried inside nested function calls. So this function is split in two:
\enumerate{
\item \code{zoning_launch_usages()} builds the tasks and returns the live
\code{mirai_map} object \code{mp} (plus the task table).
\item the caller runs \code{res <- mp[.progress]} at top level (bar shows here).
\item \code{zoning_combine_usages(res, tasks, usages)} reassembles per-usage.
\code{zoning_area_all_usages()} is kept as a convenience wrapper for when the
progress bar is not needed.
}
}
