% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_update.R
\name{cmhc_refresh_all}
\alias{cmhc_refresh_all}
\title{Run the incremental refresh across all (or some) scales — fetch only}
\usage{
cmhc_refresh_all(
  con,
  scales = cmhc_all_scales(),
  dry_run = FALSE,
  zone_qsm_path = cmhc_zone_qsm_path(),
  nbhd_qsm_path = cmhc_nbhd_qsm_path(),
  cma_to_met = NULL,
  zones_lookup = NULL,
  nbhd_matching = NULL,
  cache_dir = cmhc_cache_dir(),
  continue_on_error = TRUE
)
}
\arguments{
\item{con}{A live DBI connection.}

\item{scales}{Scales to run. Defaults to \code{\link[=cmhc_all_scales]{cmhc_all_scales()}}.}

\item{dry_run}{If \code{TRUE}, only build each scale's plan.}

\item{zone_qsm_path, nbhd_qsm_path}{Optional explicit \code{.qsm} paths.}

\item{cma_to_met, zones_lookup, nbhd_matching}{Optional pre-built references.}

\item{cache_dir}{Optional per-task \code{.qs} cache directory. Defaults to
\code{\link[=cmhc_cache_dir]{cmhc_cache_dir()}}.}

\item{continue_on_error}{If \code{TRUE} (default), keep going after a scale fails.}
}
\value{
A list with \code{plans}, \code{data}, and \code{errors}, each keyed by scale.
\code{data} is \code{NULL} when \code{dry_run = TRUE}.
}
\description{
Refreshes each requested scale in turn, building the shared references once
and reusing them across scales (\code{cma_to_met} by ZONE+NBHD, \code{zones_lookup} by
ZONE, \code{nbhd_matching} by NBHD). Performs no upload.
}
