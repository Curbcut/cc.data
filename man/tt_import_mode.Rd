% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/travel_time_duckdb.R
\name{tt_import_mode}
\alias{tt_import_mode}
\title{Import S3 Parquet into a local DuckDB file (Step 2)}
\usage{
tt_import_mode(
  mode,
  db_path = "calculated_ignore/traveltimes.duckdb",
  source_bucket = "curbcut.traveltimes",
  region = "us-east-1",
  memory_limit = "90GB",
  threads = 8L
)
}
\arguments{
\item{mode}{<\code{character}> Transport mode. One of \code{"car"}, \code{"bicycle"},
\code{"foot"}.}

\item{db_path}{<\code{character}> Path to the local DuckDB file. Created if it
does not exist. Default \code{"calculated_ignore/traveltimes.duckdb"}.}

\item{source_bucket}{<\code{character}> S3 bucket containing the final Parquet
parts. Default \code{"curbcut.traveltimes"}.}

\item{region}{<\code{character}> AWS region for \code{httpfs}. Default \code{"us-east-1"}.}

\item{memory_limit}{<\code{character}> DuckDB memory cap (e.g. \code{"90GB"}). The
sort during import can be memory-intensive; set generously. Default
\code{"90GB"}.}

\item{threads}{<\code{integer}> DuckDB thread count. Default \code{8L}.}
}
\value{
Invisibly returns the row count of the imported table.
}
\description{
Pulls all final Parquet part files for a mode from S3 and inserts them into
a persistent local DuckDB table (\verb{ttm_\{mode\}}), sorted by \code{origin_db}. This
is a one-time import per mode; subsequent queries hit the local \code{.duckdb}
file with no S3 latency.
}
\section{Idempotency}{

If the table already exists, the function prints the row count and returns
without importing. Drop the table manually to reimport:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{con <- tt_connect()
DBI::dbExecute(con, "DROP TABLE ttm_bicycle")
DBI::dbDisconnect(con, shutdown = TRUE)
tt_import_mode("bicycle")
}\if{html}{\out{</div>}}
}

\section{Disk and memory requirements}{

Peak disk usage is the final \code{.duckdb} file size plus DuckDB sort spill.
Expect roughly 1.5× the raw Parquet size on disk during import. The final
DuckDB file is typically similar in size to the source Parquet.
}

