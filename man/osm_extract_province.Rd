% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osm_places.R
\name{osm_extract_province}
\alias{osm_extract_province}
\title{Extract all categories for one province}
\usage{
osm_extract_province(
  province,
  out_dir,
  categories = osm_categories_default(),
  extra_tags = osm_extra_tags_default()
)
}
\arguments{
\item{province}{Province slug or full name.}

\item{out_dir}{Output directory.}

\item{categories}{A list of category specs (see \code{\link[=osm_categories_default]{osm_categories_default()}}).}

\item{extra_tags}{Tags to retain in the GPKG (must be a superset of those
referenced by any category). Defaults to \code{\link[=osm_extra_tags_default]{osm_extra_tags_default()}}.}
}
\value{
A named integer vector: per-category POI counts.
}
\description{
Downloads the Geofabrik extract, ensures the GPKG cache, and runs each
category in \code{categories}. One \code{.qs} file is written per (category, province)
in \code{out_dir}, with filename pattern \verb{<type_id>_<province-slug>.qs}.
}
