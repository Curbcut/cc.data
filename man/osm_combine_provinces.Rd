% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osm_places.R
\name{osm_combine_provinces}
\alias{osm_combine_provinces}
\title{Combine per-province OSM extracts into per-category files}
\usage{
osm_combine_provinces(osm_dir, out_dir, provinces = osm_provinces())
}
\arguments{
\item{osm_dir}{Directory containing the per-(category, province) \code{.qs}
files written by \code{\link[=osm_extract_canada]{osm_extract_canada()}}.}

\item{out_dir}{Directory where the combined per-category \code{.qs} files
should be written. Created if it doesn't exist.}

\item{provinces}{Province slugs used to strip suffixes. Defaults to
\code{\link[=osm_provinces]{osm_provinces()}}.}
}
\value{
A named integer vector of POI counts per category (invisibly).
}
\description{
After \code{\link[=osm_extract_canada]{osm_extract_canada()}}, \code{osm_dir} contains one \code{.qs} per
(category, province) — e.g. ~154 files for 13 provinces × 12 categories.
This function rebinds them into one \code{.qs} per category, normalizing each
to \verb{(name, geometry)}, so the destination integration script can read
them the same way Foursquare files are read.
}
