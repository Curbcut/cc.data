% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_data.R
\name{cmhc_run_and_collect}
\alias{cmhc_run_and_collect}
\title{Run CMHC tasks and collect results (parallel or sequential, RAM or disk)}
\usage{
cmhc_run_and_collect(
  tasks,
  .worker,
  par_env_objs,
  output_dir = NULL,
  label = "cmhc"
)
}
\arguments{
\item{tasks}{A data.frame of tasks (one row per unit of work) as produced by
\code{cmhc_build_tasks()}. Column names must match \code{.worker}'s formals.}

\item{.worker}{The worker function (closure).}

\item{par_env_objs}{A named list of objects for \code{cmhc_make_par_env()}.}

\item{output_dir}{Optional directory (or S3 prefix) to save per-task \code{.qs}
files. When \code{NULL}, results are accumulated in RAM.}

\item{label}{A short string for progress messages (e.g. \code{"cmhc_get_annual_ct"}).}
}
\value{
When \code{output_dir} is \code{NULL}: a named list of combined data.frames.
When \code{output_dir} is set: the directory path (character).
}
\description{
Common execution logic shared by all \verb{cmhc_get_*} functions. Handles the
parallel vs sequential decision, the optional disk-save mode (\code{output_dir}),
and the final combine step.
}
\details{
When \code{output_dir} is set, each task result is saved as a small \code{.qs} file
(to local disk or S3 when \code{options(cmhc.s3_bucket)} is set) and the function
returns the path/prefix — nothing is accumulated in RAM.
Use \code{cmhc_read_variable(dir, key)} afterwards to read one variable at a time.
}
