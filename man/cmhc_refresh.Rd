% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_update.R
\name{cmhc_refresh}
\alias{cmhc_refresh}
\title{Run the incremental refresh for ONE scale (fetch only — no upload)}
\usage{
cmhc_refresh(
  con,
  scale,
  uids = NULL,
  registry = cmhc_registry(),
  levels = cmhc_levels(),
  refs = NULL,
  cache_dir = cmhc_cache_dir(),
  dry_run = FALSE
)
}
\arguments{
\item{con}{A live DBI connection (e.g. \code{cc.pipe::db_connect()}).}

\item{scale}{One of \code{"cma"}, \code{"csd"}, \code{"ct"}, \code{"zone"}, \code{"nbhd"}.}

\item{uids}{Optional geo (or CMA parent) UIDs. Defaults to the scale's own.}

\item{registry}{Request registry. Defaults to \code{\link[=cmhc_registry]{cmhc_registry()}}.}

\item{levels}{Scale config table. Defaults to \code{\link[=cmhc_levels]{cmhc_levels()}}.}

\item{refs}{Optional pre-built shared references. Built on demand when \code{NULL}.}

\item{cache_dir}{Optional directory for per-task \code{.qs} caching. When set, a
re-run skips tasks already cached and retries only transient failures, so a
crash mid-run is resumable. Defaults to \code{\link[=cmhc_cache_dir]{cmhc_cache_dir()}}.}

\item{dry_run}{If \code{TRUE}, return only the plan.}
}
\value{
A list with \code{plan} and \code{data}, where
\code{data[[variable_id]][[freq]]} is a named list of long frames
(\code{id}, \code{time}, \code{geo_vintage}, \code{value}).
}
\description{
Builds the plan, then fetches every period flagged \code{to_fetch}, returning
upload-ready long frames. Requires running mirai daemons for a real parallel
fetch. Performs no upload.
}
