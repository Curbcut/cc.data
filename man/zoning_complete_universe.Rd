% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zoning.R
\name{zoning_complete_universe}
\alias{zoning_complete_universe}
\title{Complete usage results with 0 (collected) or NA (not collected)}
\usage{
zoning_complete_universe(areas, da_universe, collected_csds)
}
\arguments{
\item{areas}{A named list of tibbles (\code{id}, \code{value}), one per usage, as
returned by \code{zoning_area_all_usages()}.}

\item{da_universe}{A tibble (\code{id}, \code{csd_id}) from \code{zoning_da_universe()}.}

\item{collected_csds}{Character vector of CSD UIDs present in the zoning
data (i.e. \code{unique(zones$csd_id)}).}
}
\value{
A named list of tibbles (\code{id}, \code{value}), one per usage, completed
over the universe with 0 / NA.
}
\description{
For each usage, expands results to the full DA universe of the in-scope
CMAs. A DA whose CSD appears in the collected zoning (so the city was
processed) gets 0 when the usage is absent — the absence is real. A DA
whose CSD was not collected gets NA — the data is missing. DAs outside the
universe (outside the scope CMAs) are not included.
}
