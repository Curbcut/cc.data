% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osm_places.R
\name{osm_extract_canada}
\alias{osm_extract_canada}
\title{Extract OSM POIs for all of Canada (parallel)}
\usage{
osm_extract_canada(
  out_dir,
  provinces = osm_provinces(),
  categories = osm_categories_default(),
  extra_tags = osm_extra_tags_default(),
  n_workers = 4
)
}
\arguments{
\item{out_dir}{Output directory.}

\item{provinces}{Character vector of provinces. Defaults to all 13 via
\code{\link[=osm_provinces]{osm_provinces()}}.}

\item{categories}{List of category specs (see \code{\link[=osm_categories_default]{osm_categories_default()}}).}

\item{extra_tags}{Extra OSM tags to retain.}

\item{n_workers}{Number of parallel workers. Set to 1 to run serially.}
}
\value{
A data frame: one row per province, with category counts and a
\code{total} column.
}
\description{
Runs \code{\link[=osm_extract_province]{osm_extract_province()}} across all provinces. Uses \code{mirai} daemons
when \code{n_workers > 1}; serial otherwise. Output is one \code{.qs} per
(category, province) under \code{out_dir}.
}
