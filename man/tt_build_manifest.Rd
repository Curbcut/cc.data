% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/travel_time_duckdb.R
\name{tt_build_manifest}
\alias{tt_build_manifest}
\title{Build the origin manifest for a travel-time mode (Step 1a)}
\usage{
tt_build_manifest(
  mode,
  source_bucket = "curbcut.routing",
  source_prefix = "2026",
  rows_per_part = 25000000,
  min_batch_size = 1000,
  manifest_dir = "calculated_ignore"
)
}
\arguments{
\item{mode}{<\code{character}> Transport mode. One of \code{"car"}, \code{"bicycle"},
\code{"foot"}.}

\item{source_bucket}{<\code{character}> S3 bucket containing the raw \code{.qs}
batches. Default \code{"curbcut.routing"}.}

\item{source_prefix}{<\code{character}> Key prefix (year folder) inside
\code{source_bucket}. Default \code{"2026"}.}

\item{rows_per_part}{<\code{numeric}> Target maximum number of
origin→destination rows per output part file. Default \code{25e6}.}

\item{min_batch_size}{<\code{integer}> Minimum file size in bytes; smaller files
are assumed empty and skipped. Default \code{1000}.}

\item{manifest_dir}{<\code{character}> Local directory where the manifest \code{.qs}
file is written. Default \code{"calculated_ignore"}.}
}
\value{
Invisibly returns the manifest as a \code{data.table} with columns
\code{origin_db}, \code{n_rows}, \code{s3_key}, \code{cum_rows}, \code{part_num}.
}
\description{
Scans all \code{.qs} batch files for a given transport mode in the source S3
bucket, records which origin IDs live in each batch file, and assigns each
origin to a numbered output "part" so that each part holds at most
\code{rows_per_part} rows across all origins in it.
}
\details{
The manifest is saved as \verb{manifest_\{mode\}.qs} in \code{manifest_dir}. If the
file already exists the function loads and returns it without re-scanning —
delete the file manually to force a rebuild.
}
\section{Resume behaviour}{

If \verb{manifest_\{mode\}.qs} exists in \code{manifest_dir}, the manifest is loaded
from disk and returned immediately. This lets you safely kill and resume
the build pipeline without re-scanning all batch files.
To force a fresh scan, delete the file:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{file.remove("calculated_ignore/manifest_car.qs")
tt_build_manifest("car")
}\if{html}{\out{</div>}}
}

