% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zoning.R
\name{zoning_area_for_usage}
\alias{zoning_area_for_usage}
\title{Compute per-DA land area for a single usage}
\usage{
zoning_area_for_usage(zones, da, usage)
}
\arguments{
\item{zones}{An \code{sf} object of zoning polygons (with \code{usage_classification}).}

\item{da}{An \code{sf} object of dissemination areas with an \code{id} column.}

\item{usage}{Character. The usage type to compute.}
}
\value{
A tibble with columns \code{id} and \code{value} (land area in m2). Returns an
empty tibble when no zone permits the usage or no DA intersects it.
}
\description{
Zones permitting \code{usage} are dissolved into one geometry (\code{st_union}) BEFORE
intersecting with the dissemination areas. Dissolving first is essential:
when two zones permitting the same usage overlap, summing per-zone
intersections counts the shared ground more than once. The union collapses
overlaps so each parcel of ground is counted exactly once per usage.
}
\keyword{internal}
