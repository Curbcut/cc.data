% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zoning.R
\name{zoning_attach_cma}
\alias{zoning_attach_cma}
\title{Attach the parent CMA id to zones and DAs}
\usage{
zoning_attach_cma(zones, da, cma_ids, dataset = "CA21")
}
\arguments{
\item{zones}{An \code{sf} object with a \code{csd_id} column.}

\item{da}{An \code{sf} object with an \code{id} column.}

\item{cma_ids}{Character vector of in-scope CMA UIDs.}

\item{dataset}{Character. cancensus dataset code (default "CA21").}
}
\value{
A list with \code{zones} and \code{da}, each gaining a \code{cma_id} column.
}
\description{
Adds a \code{cma_id} column to both the zones and the DA \code{sf} objects, using the
DA-universe (cancensus) CSD/CMA hierarchy. DAs are matched on \code{id}; zones on
\code{csd_id}. This is what lets \code{zoning_area_all_usages()} chunk by CMA without
any spatial join.
}
