% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_data.R
\name{cmhc_fetch_csd_historical_data}
\alias{cmhc_fetch_csd_historical_data}
\title{Fetch Historical Time Series for an Individual CSD}
\usage{
cmhc_fetch_csd_historical_data(
  survey,
  series,
  dimension,
  geo_uid,
  frequency = "Annual"
)
}
\arguments{
\item{survey}{Character. CMHC survey code (e.g., "Rms").}

\item{series}{Character. Series to extract (e.g., "Vacancy Rate").}

\item{dimension}{Character. Breakdown dimension (e.g., "Bedroom Type").}

\item{geo_uid}{Character. CSD GeoUID (7-digit, e.g. "2465005").}

\item{frequency}{Character. "Annual" or "Monthly".}
}
\value{
A raw CMHC data.frame, or NULL if the CSD has no data.
}
\description{
Uses \code{breakdown = "Historical Time Periods"} to retrieve all available
years for one CSD in a single API call — the same endpoint used for CMA
historical queries. The CMHC API recognises CSD-length geo_uids and
returns a \code{DateString}-format response (identical to CMA historical).
}
