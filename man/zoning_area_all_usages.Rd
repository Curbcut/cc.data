% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zoning.R
\name{zoning_area_all_usages}
\alias{zoning_area_all_usages}
\title{Compute per-DA land area for every usage (wrapper, no live progress bar)}
\usage{
zoning_area_all_usages(zones, da, usages = ZONING_USAGE_TYPES)
}
\arguments{
\item{zones}{An \code{sf} object of zoning polygons. Must have a \code{cma_id} column.}

\item{da}{An \code{sf} object of dissemination areas with \code{id} and \code{cma_id}.}

\item{usages}{Character vector of usage types (default \code{ZONING_USAGE_TYPES}).}
}
\value{
A named list of tibbles (\code{id}, \code{value}), one per usage.
}
\description{
Convenience wrapper around \code{zoning_launch_usages()} +
\code{zoning_combine_usages()}. The green ETA bar will NOT show through this
wrapper because \verb{[.progress} is not at top level — use the two-step form in
a script when you want the bar.
}
