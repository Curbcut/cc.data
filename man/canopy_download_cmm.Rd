% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canopy.R
\name{canopy_download_cmm}
\alias{canopy_download_cmm}
\title{Download CMM canopy tiles for a given year}
\usage{
canopy_download_cmm(year = 2023L, out_dir = "data/cmm_canopy")
}
\arguments{
\item{year}{Integer. Canopy index year (e.g., 2023).}

\item{out_dir}{Character. Local directory where the unzipped folders will
be stored. Created if it does not exist.}
}
\value{
Invisibly, a character vector of unzipped directory paths (one per
ZIP archive).
}
\description{
Downloads all ZIP archives of the canopy index for a given year from the
Observatoire de la CMM web page, unzips them under \code{out_dir}, and
deletes the ZIP files.
}
\details{
Internally, the function scrapes the HTML page and keeps only URLs whose
filename matches \code{IndiceCanopee_<year>_TIF.zip}.
}
