% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_data.R
\name{cmhc_make_par_env}
\alias{cmhc_make_par_env}
\title{Build a self-contained environment for mirai daemon serialization}
\usage{
cmhc_make_par_env(...)
}
\arguments{
\item{...}{Named objects (functions, data, constants) the worker needs.}
}
\value{
An environment with parent \code{globalenv()}.
}
\description{
When a closure is serialized for mirai, package-namespace functions resolve
to the INSTALLED version on the daemon, not the \code{load_all()} dev version.
This helper creates an environment that contains all needed objects and
rebinds every function's enclosing environment so internal references
(e.g. \code{cmhc_clean_names}, \code{CMHC_PCT_SERIES}) resolve from the environment
rather than the installed namespace.
}
