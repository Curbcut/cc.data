% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/travel_time_duckdb.R
\name{tt_shred_to_s3}
\alias{tt_shred_to_s3}
\title{Shred .qs batches into per-part Parquet fragments on S3 (Step 1b)}
\usage{
tt_shred_to_s3(
  mode,
  source_bucket = "curbcut.routing",
  dest_bucket = "curbcut.traveltimes",
  manifest_dir = "calculated_ignore"
)
}
\arguments{
\item{mode}{<\code{character}> Transport mode. One of \code{"car"}, \code{"bicycle"},
\code{"foot"}.}

\item{source_bucket}{<\code{character}> S3 bucket containing the raw \code{.qs}
batches. Default \code{"curbcut.routing"}.}

\item{dest_bucket}{<\code{character}> S3 bucket for output fragments and final
parts. Default \code{"curbcut.traveltimes"}.}

\item{manifest_dir}{<\code{character}> Directory where \verb{manifest_\{mode\}.qs} was
written by \code{\link[=tt_build_manifest]{tt_build_manifest()}}. Default \code{"calculated_ignore"}.}
}
\value{
Called for side effects. Stops with an error if shredding is
incomplete at the end (safe guard against premature merging).
}
\description{
Reads each raw \code{.qs} batch from the source bucket, flattens it into a
long-format \code{data.table}, assigns each row to the part number determined by
the manifest, and uploads one Parquet fragment per part to the destination
bucket. Each fragment is compressed with Zstandard level 3.
}
\details{
Already-uploaded batches are detected via S3 key names and skipped, so the
function resumes cleanly after interruptions. A final consistency check
aborts with an error if any batch is still missing — preventing a premature
call to \code{\link[=tt_merge_parts]{tt_merge_parts()}}.
}
\section{Fragment layout}{

Fragments are uploaded to
\verb{ttm_frags/mode=\{mode\}/part-\{NNNNN\}/\{batch_label\}.parquet}.
\code{tt_merge_parts()} reads this prefix to collect all fragments for each
part.
}

\section{ID encoding}{

\code{origin_db} and \code{dest_db} are cast to \code{INT64} via \code{bit64::as.integer64()}
before writing. This maps to \code{UBIGINT} in Parquet and avoids the
scientific-notation corruption that occurs when 11-digit IDs pass through
R's \code{double} type.
}

