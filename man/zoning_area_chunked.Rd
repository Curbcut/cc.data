% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zoning.R
\name{zoning_area_chunked}
\alias{zoning_area_chunked}
\title{Compute per-DA land area for every usage, in chunks (CMHC-style)}
\usage{
zoning_area_chunked(zones, da, usages = ZONING_USAGE_TYPES, chunk_size = 48L)
}
\arguments{
\item{zones}{An \code{sf} object of zoning polygons (needs \code{cma_id}).}

\item{da}{An \code{sf} object of dissemination areas (needs \code{id}, \code{cma_id}).}

\item{usages}{Character vector of usage types (default \code{ZONING_USAGE_TYPES}).}

\item{chunk_size}{Integer. Tasks per chunk (default 48).}
}
\value{
A named list of tibbles (\code{id}, \code{value}), one per usage.
}
\description{
Runs the (usage x CMA) tasks in chunks. Each chunk gets its own native green
\verb{[.progress} bar with ETA, and a one-line summary is printed after each chunk
(how many done, elapsed time) — the same pattern as the CMHC pipeline. A
\code{gc()} runs between chunks to keep memory flat. Combines results per usage at
the end.
}
\details{
Because the green bar only renders at top level, this function calls
\code{mp[.progress]} directly (it is itself meant to be called at the top level of
a script, which is the normal case).
}
