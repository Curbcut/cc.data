% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_data.R
\name{cmhc_fetch_ct_historical_data}
\alias{cmhc_fetch_ct_historical_data}
\title{Fetch Historical Time Series for an Individual CT}
\usage{
cmhc_fetch_ct_historical_data(
  survey,
  series,
  dimension,
  geo_uid,
  frequency = "Annual"
)
}
\arguments{
\item{survey}{Character. CMHC survey code (e.g., "Rms").}

\item{series}{Character. Series to extract (e.g., "Vacancy Rate").}

\item{dimension}{Character. Breakdown dimension (e.g., "Bedroom Type").}

\item{geo_uid}{Character. CT UID in short format (e.g. "9330010.02").}

\item{frequency}{Character. "Annual" or "Monthly".}
}
\value{
A raw CMHC data.frame, or NULL if the CT has no data.
}
\description{
Uses \code{breakdown = "Historical Time Periods"} to retrieve all available
years for one Census Tract in a single API call — the same endpoint used
for CMA and CSD historical queries. The CMHC API accepts CT UIDs in the
short format (e.g. "9330010.02") and returns a \code{DateString}-format
response (identical to CMA historical).
}
