% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/travel_time_duckdb.R
\name{tt_query_s3}
\alias{tt_query_s3}
\title{Query travel times directly from S3 Parquet using the origin-range index (Step 3c)}
\usage{
tt_query_s3(
  con,
  origin_ids,
  dest_ids = NULL,
  mode = "bicycle",
  max_time_s = NULL
)
}
\arguments{
\item{con}{<\code{DBIConnection}> Connection returned by \code{\link[=tt_connect_s3]{tt_connect_s3()}}.}

\item{origin_ids}{<\code{character}|\code{integer64}|\code{numeric}> Vector of origin
dissemination-block UIDs to query.}

\item{dest_ids}{<\code{character}|\code{integer64}|\code{numeric}|\code{NULL}> Optional
destination UID filter. \code{NULL} returns all reachable destinations.}

\item{mode}{<\code{character}> Transport mode. One of \code{"car"}, \code{"bicycle"},
\code{"foot"}. Default \code{"bicycle"}.}

\item{max_time_s}{<\code{integer}|\code{NULL}> Optional upper bound on travel time in
seconds.}
}
\value{
A list with two elements:
\describe{
\item{\code{data}}{A \code{data.frame} with columns \code{origin_db}, \code{dest_db},
\code{time_s}, \code{dist_m}.}
\item{\code{elapsed_sec}}{Query wall-clock time in seconds.}
}
}
\description{
Uses the index attached to \code{con} by \code{\link[=tt_connect_s3]{tt_connect_s3()}} to identify which
part files overlap the requested origins, then issues a single DuckDB
\verb{read_parquet([...])} query against only those files. Because part files are
non-overlapping and sorted by \code{origin_db}, the prune is exact: no false
positives, no full-table scans.
}
\section{Query performance — why it's fast}{

\enumerate{
\item \strong{File pruning via the index}: the index holds min/max
\code{origin_db} per part file (derived from Parquet footer stats, not row
data). \code{tt_query_s3()} filters to files whose range overlaps the
query's \verb{[min(origin_ids), max(origin_ids)]}. For geographically
compact origin sets (same CSD) this is almost always 1 file.
\item \strong{Row-group skipping via zone maps}: within each opened file,
DuckDB reads the footer's per-row-group min/max for \code{origin_db} and
skips row groups whose range doesn't overlap the query. Only matching
row groups are decompressed.
\item \strong{Predicate pushdown}: the \verb{WHERE origin_db IN (...)} clause is
pushed into \code{read_parquet()}, so DuckDB does not materialise the full
file before filtering.
}
Together these reduce a multi-billion-row dataset to a sub-second scan
for typical single-municipality queries.
}

\section{ID encoding}{

\code{origin_ids} (and \code{dest_ids} if provided) are cast to
\code{bit64::integer64()} before min/max computation and to character before
SQL interpolation, preventing scientific-notation corruption of 11-digit
UIDs.
}

\section{Lambda usage example}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Cold-start setup (once per container)
con <- tt_connect_s3(memory_limit = "4GB", threads = 2L)

# Per-invocation query
handler <- function(event, context) \{
  res <- tt_query_s3(
    con,
    origin_ids = event$origin_ids,
    mode       = event$mode,
    max_time_s = 1800L   # 30-minute isochrone
  )
  jsonlite::toJSON(res$data)
\}
}\if{html}{\out{</div>}}
}

\seealso{
\code{\link[=tt_connect_s3]{tt_connect_s3()}}, \code{\link[=tt_build_index]{tt_build_index()}}
}
