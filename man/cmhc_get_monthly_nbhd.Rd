% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_data.R
\name{cmhc_get_monthly_nbhd}
\alias{cmhc_get_monthly_nbhd}
\title{Retrieve Monthly CMHC Data at the Neighbourhood Level}
\usage{
cmhc_get_monthly_nbhd(
  requests,
  cma_uids = NULL,
  cma_to_met = NULL,
  qsm_path,
  output_dir = NULL
)
}
\arguments{
\item{requests}{A list of request objects. Each must include \code{survey},
\code{series}, \code{dimension}, \code{years} (numeric vector), and \code{months}
(character or integer vector, e.g. \code{sprintf("\%02d", 1:12)}).}

\item{cma_uids}{Character vector of CMA GeoUIDs. Defaults to all CMA
GeoUIDs known to CMHC via \code{\link[=cmhc_get_geo_uids]{cmhc_get_geo_uids()}}.}

\item{cma_to_met}{A \code{data.frame} mapping \code{cma_uid} to \code{met_code}. Defaults
to \code{\link[=cmhc_get_cma_to_met]{cmhc_get_cma_to_met()}}.}

\item{qsm_path}{Character. Path to the \code{.qsm} bundle of harmonized
shapefiles.}

\item{output_dir}{Optional. Directory (or S3 prefix) for caching per-task
\code{.qs} files.}
}
\value{
A named list \code{nbhd} containing reshaped data frames for each
variable, with monthly \code{time} values (e.g. \code{"202210"}).
}
\description{
Like \code{\link[=cmhc_get_annual_nbhd]{cmhc_get_annual_nbhd()}} but expands over both years AND months.
Each task is one (CMA × request × year × month) combination, requiring
significantly more API calls (~12x as many as annual). Use with
\code{output_dir} for caching when running large month ranges.
}
