% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmhc_data.R
\name{cmhc_match_zone_name}
\alias{cmhc_match_zone_name}
\title{Match an API Survey Zone Name to a Shapefile zone_id}
\usage{
cmhc_match_zone_name(zone_name, met_code, zones_lookup)
}
\arguments{
\item{zone_name}{Character. The API zone name to match.}

\item{met_code}{Character. The CMA's MET_CODE (from CMHC translation).}

\item{zones_lookup}{A data.frame with columns \code{zone_id},
\code{met_code}, \code{zone_name}, and pre-computed \code{n_norm}.}
}
\value{
A list with \code{zone_id}, \code{met_code}, and \code{method}.
}
\description{
Uses a 3-pass matching strategy against a normalized lookup of the
CMHC reference shapefile:
\enumerate{
\item Exact match (or first-part split on "/") within the same MET_CODE.
\item Exact match across all MET_CODEs -- prefers MET_CODEs with the
fewest zones (single-zone CMAs like Joliette, Drummondville).
\item Fuzzy match within same MET_CODE (Levenshtein <= 4 &
Jaro-Winkler >= 0.85).
}
}
