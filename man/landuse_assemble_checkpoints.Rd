% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landuse.R
\name{landuse_assemble_checkpoints}
\alias{landuse_assemble_checkpoints}
\title{Assemble per-file landuse checkpoints into a single plug-and-play .qs}
\usage{
landuse_assemble_checkpoints(
  ckpt_dir = paste0(Sys.getenv("CURBCUT_DATA_SHARING_PATH"),
    "cc.data/landuse_ckpt"),
  out_path = paste0(Sys.getenv("CURBCUT_DATA_SHARING_PATH"),
    "cc.data/landuse.qs"),
  aggregate = TRUE,
  progress = TRUE
)
}
\arguments{
\item{ckpt_dir}{Character. Root directory containing one subdirectory
per scale (e.g. \code{landuse_ckpt/CMA}, \code{landuse_ckpt/DB}). Each
subdirectory should contain \verb{<cma_id>_<year>.qs} checkpoint files
written by \code{\link[=build_landuse_from_file]{build_landuse_from_file()}}. Defaults to the canonical shared
location \code{CURBCUT_DATA_SHARING_PATH/cc.data/landuse_ckpt}.}

\item{out_path}{Character or NULL. If a path is supplied, the assembled
list is written there with \code{qs::qsave}. Defaults to the canonical
shared location \code{CURBCUT_DATA_SHARING_PATH/cc.data/landuse.qs}.}

\item{aggregate}{Logical. If TRUE (default), run the same dedup +
aggregation + pct-computation pipeline as \code{\link[=build_landuse_from_file]{build_landuse_from_file()}},
so each scale's data.table is one row per \verb{(id, year)} with seven
\verb{landuse_*} area columns (m^2) and seven \verb{landuse_*_pct} columns
(summing to 1.0). If FALSE, return the raw \code{rbindlist}-ed checkpoint
rows (preserves the \code{cma_id} column, may contain multiple rows per
\verb{(id, year)} when a polygon's bbox intersects more than one CMA's
raster).}

\item{progress}{Logical. Whether to print per-scale progress messages.}
}
\value{
A named list with one entry per scale subdirectory of
\code{ckpt_dir}, each a \code{data.table}. Invisibly returned when \code{out_path}
is supplied.
}
\description{
Reads every per-file checkpoint produced by \code{\link[=build_landuse_from_file]{build_landuse_from_file()}}
(via its \code{checkpoint_dir} argument), rbinds them per scale, applies the
same dedup + aggregation + percentage pipeline used inside the build
function, and returns a named list of one aggregated data.table per
scale subdirectory. Optionally writes the result to a single \code{.qs} file
so downstream code can load one artifact instead of walking a tree of
per-file caches.
}
