% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/travel_time_matrix.R
\name{build_origin_requests}
\alias{build_origin_requests}
\title{Build OSRM request metadata for a single origin}
\usage{
build_origin_requests(
  input,
  centroids_proj,
  all_ids,
  x_vec,
  y_vec,
  max_dist,
  chunk_size_url,
  routing_server,
  profile,
  requests_dir
)
}
\arguments{
\item{input}{<\code{list}> Single origin specification with elements:
\itemize{
\item \code{origin_idx}: Integer index position in the centroids dataset
\item \code{origin_id}: Character ID of the origin point
}}

\item{centroids_proj}{<\code{sf}> All centroids transformed to projected CRS
(EPSG:3347) for distance calculations}

\item{all_ids}{<\code{character}> Vector of all centroid IDs (same order as
\code{centroids_proj})}

\item{x_vec}{<\code{numeric}> Named vector of longitudes (WGS84), names are IDs}

\item{y_vec}{<\code{numeric}> Named vector of latitudes (WGS84), names are IDs}

\item{max_dist}{<\code{numeric}> Maximum distance in meters for neighbor inclusion}

\item{chunk_size_url}{<\code{integer}> Maximum number of destinations per OSRM
request URL (typically \code{max_url_coords - 1} to account for origin)}

\item{routing_server}{<\code{character}> OSRM server base URL (must end with \code{/})}

\item{profile}{<\code{character}> OSRM routing profile (e.g., "car", "bicycle")}

\item{requests_dir}{<\code{character}> Directory path for saving request metadata
files}
}
\value{
List with elements:
\itemize{
\item \code{origin_id}: Character ID of the processed origin
\item \code{status}: One of "success", "isolated", or "already_exists"
\item \code{n_requests}: Integer count of OSRM requests generated (NA if already
existed)
}
}
\description{
Worker function for use with \code{dispatch_model()} to compute spatial neighbors
and construct OSRM API request URLs for one origin point. Results are saved
to disk as \verb{\{origin_id\}.qs} files containing request metadata (URLs,
destination IDs, coordinates).
}
\details{
This function is called in parallel by \code{dispatch_model()}, with each worker
processing one origin independently. The spatial neighbor computation
(via \code{sf::st_is_within_distance}) is done per-origin to avoid holding all
neighbor relationships in memory simultaneously.
\subsection{Processing logic}{
\enumerate{
\item \strong{Resume detection}: Checks if \verb{\{origin_id\}.qs} already exists. If yes,
returns immediately without recomputation.
\item \strong{Neighbor computation}: Uses \code{sf::st_is_within_distance()} to find all
points within \code{max_dist} meters of this origin (excluding self).
\item \strong{Isolated origins}: Origins with no neighbors get a single "isolated"
request record (no URL, empty dest_ids).
\item \strong{Request chunking}: If an origin has more neighbors than
\code{chunk_size_url}, destinations are split across multiple OSRM requests.
Each request includes the origin coordinates plus up to \code{chunk_size_url}
destination coordinates.
\item \strong{URL construction}: Builds OSRM table API URLs with format:
\verb{\{server\}table/v1/\{profile\}/\{coords\}?sources=0&annotations=distance,duration}
where coords are semicolon-separated \verb{lon,lat} pairs (origin first).
\item \strong{Atomic save}: Request metadata saved to disk via \code{qs::qsave()} for
later batch fetching.
}
}

\subsection{Output file format}{

Each \verb{\{origin_id\}.qs} file contains a list of request records. Each record:
\itemize{
\item \code{origin_id}: Character ID of origin
\item \code{dest_ids}: Character vector of destination IDs in this request
\item \code{url}: Character OSRM API URL (or NULL if isolated)
\item \code{chunk_index}: Integer chunk number (for multi-chunk origins)
\item \code{isolated}: Logical, TRUE only for origins with no neighbors
}
}
}
\keyword{internal}
