% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canopy.R
\name{canopy_load_tiles}
\alias{canopy_load_tiles}
\title{Load and mosaic CMM canopy tiles}
\usage{
canopy_load_tiles(tif_dir, out_raster = NULL, overwrite = FALSE)
}
\arguments{
\item{tif_dir}{Character. Root directory where all unzipped canopy TIFF
tiles are stored.}

\item{out_raster}{Optional character path to a raster file (e.g. ".tif")
where the mosaic will be written using \code{terra::writeRaster()}.
If \code{NULL}, nothing is written.}

\item{overwrite}{Logical. If \code{TRUE}, overwrite \code{out_raster}
if it already exists.}
}
\value{
A \code{SpatRaster} mosaic of all valid canopy tiles.
}
\description{
Given a directory containing all unzipped canopy TIFF folders/files
from the CMM, this function:
\itemize{
\item finds all .tif files recursively
\item reads them as SpatRaster tiles
\item drops invalid tiles
\item checks that all tiles share the same CRS
\item mosaics them into a single SpatRaster
\item optionally writes the mosaic to disk
}
}
